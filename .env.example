FLASK_APP=run.py
FLASK_DEBUG=1
HOST=127.0.0.1
PORT=5000

# Leave empty to use instance/tasks.db (SQLite).
# PostgreSQL example (install the psycopg driver first):
# DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/tasks
DATABASE_URL=
